"""
Configuration package.

Overview:
- Holds task-specific configuration files (YAML) for FGVC (CUB) and Re-ID (VeRi).
- These are consumed by `dcal.utils.config` for building models, data loaders,
  losses, and trainers.

References:
- See `dual_cross_attention_learning.md` for hyperparameters like R, image sizes,
  training schedules, and loss weighting strategy.
"""


def _module_overview() -> None:
    """
    Stub to satisfy the requirement of having at least one callable.
    """
    pass


